category: network_access
name: "AWS SDK service connection via boto3"
confidence: 0.95
# cap_id is a prefix. Final ID = ast:{lang}:{cap_id}:{service-name}
# Example: ast:python:boto3:client:s3  or  ast:python:boto3:client:dynamodb
# Distinct service names produce distinct capability IDs, enabling fine-grained
# manifest tracking and IAM policy generation per detected service.
cap_id: "boto3:client"
symbol_capture: "service"
